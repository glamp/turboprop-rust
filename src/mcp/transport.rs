//! MCP transport layer
//!
//! Placeholder for future MCP transport implementation

/// Transport layer for MCP communication
pub struct Transport;

impl Transport {
    /// Create a new transport
    pub fn new() -> Self {
        Self
    }
}

impl Default for Transport {
    fn default() -> Self {
        Self::new()
    }
}